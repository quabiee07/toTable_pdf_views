import 'package:flutter/material.dart';

class PdfViewBody extends StatelessWidget {
  const PdfViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: [],
      ),
    );
  }
}
